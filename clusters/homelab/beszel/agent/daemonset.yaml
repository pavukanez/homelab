apiVersion: apps/v1
kind: DaemonSet
metadata:
  name: beszel-agent
  namespace: beszel
  labels:
    app: beszel-agent
spec:
  selector:
    matchLabels:
      app: beszel-agent
  template:
    metadata:
      labels:
        app: beszel-agent
    spec:
      hostNetwork: true
      containers:
      - name: agent
        image: henrygd/beszel-agent:latest
        args:
        - -key
        - "$(KEY)"
        env:
        - name: KEY
          value: "PLACEHOLDER_KEY_REPLACE_ME"
        - name: PORT
          value: "45876"
        ports:
        - containerPort: 45876
          hostPort: 45876
        volumeMounts:
        - name: host-root
          mountPath: /extra-filesystems/root
          readOnly: true
      volumes:
      - name: host-root
        hostPath:
          path: /
      tolerations:
      - effect: NoSchedule
        key: node-role.kubernetes.io/master
        operator: Exists
      - effect: NoSchedule
        key: node-role.kubernetes.io/control-plane
        operator: Exists
      - operator: Exists  # Allow running on all nodes
